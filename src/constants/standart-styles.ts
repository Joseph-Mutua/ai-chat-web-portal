import { StyleSheet } from 'react-native'

import { Theme } from './Colors'

export const standartStyles = StyleSheet.create({
  container: {
    paddingHorizontal: 16,
    paddingVertical: 8,
    backgroundColor: Theme.brand.white,
  },
})
